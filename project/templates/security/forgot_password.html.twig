{% extends 'base.html.twig' %}

{% block title %}{{ 'reset.request.title'|trans }}{% endblock %}

{% block content %}
<section class="">
    <div class="container h-100">
        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="card" style="border-radius: 1rem; max-width: 30vw;">
                <div class="row g-0">
                    <div class="d-flex align-items-center">
                        <div class="card-body p-4 p-lg-5 text-black">

                            {% for type, messages in app.flashes %}
                                {% for message in messages %}
                                    <div class="alert alert-{{ type }} alert-dismissible fade show" role="alert">
                                        {{ message|trans }}
                                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                                    </div>
                                {% endfor %}
                            {% endfor %}

                            <h5 class="fw-normal mb-3 pb-3" style="letter-spacing: 1px;">{{ 'reset.request.subtitle'|trans }}</h5>
                            <p class="text-muted mb-4">{{ 'reset.request.description'|trans }}</p>

                            {{ form_start(form) }}
                                <div class="form-outline mb-4">
                                    {{ form_row(form.email) }}
                                </div>
                                {{ form_row(form.submit) }}
                            {{ form_end(form) }}

                            <p class="mt-3">
                                <a href="{{ path('app.login', {_locale: app.request.locale}) }}">{{ 'reset.request.back_to_login'|trans }}</a>
                            </p>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}