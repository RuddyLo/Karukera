{{ form_start(form) }}
    <div class="row">
        {{ form_row(form.platform) }}
        {{ form_row(form.url) }}
        {{ form_row(form.icon) }}
    </div>

    <button class="btn btn-secondary mt-4 ml-2" type="button" onclick="history.back()">Retour</button>
    <button class="btn btn-success mt-4 ml-2" type="submit">Enregistrer</button>
{{ form_end(form) }}
