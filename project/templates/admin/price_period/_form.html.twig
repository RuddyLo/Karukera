<div class="mb-4">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
    <a class="btn btn-info text-white" href="{{ path('price_period_index') }}">Retour</a>
{{ form_end(form) }}

</div>