<form method="post" action="{{ path('price_period_delete', {'id': price_period.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ price_period.id) }}">
    <button class="btn">Delete</button>
</form>
